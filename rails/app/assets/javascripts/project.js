/*
var testName = function () {
    if($('input#project_name').text()==="") {
        console.log("Project name must be defined!");
        return false;
    }
    else return true;
};
$(document).ready(function() {
    $("#new_project").on("ajax:success", function(e,data,status,xhr) {
       $("#new_project").append(xhr.responseText);
    }).on("ajax:error",function(e,xhr,status,error){
        $("#new_project").append("<p>ERROR</p>")
    });
});
*/